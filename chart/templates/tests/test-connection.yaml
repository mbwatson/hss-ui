apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "renci-chatbot.fullname" . }}-test-connection"
  labels:
    {{- include "renci-chatbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "renci-chatbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
